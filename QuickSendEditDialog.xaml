<Window x:Class="Comm.QuickSendEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑快捷按钮"
        Height="240"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="Height"
        MinHeight="200">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 名称 -->
        <TextBlock Grid.Row="0" Grid.Column="0"
                   Text="名称："
                   VerticalAlignment="Center"
                   Margin="0,0,10,5"
                   FontSize="14"/>
        <TextBox x:Name="txtName"
                 Grid.Row="0" Grid.Column="1"
                 Height="32"
                 VerticalContentAlignment="Center"
                 FontSize="14"
                 Margin="0,0,0,10"/>

        <!-- 内容 -->
        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="内容："
                   VerticalAlignment="Center"
                   Margin="0,0,10,5"
                   FontSize="14"/>
        <TextBox x:Name="txtContent"
                 Grid.Row="1" Grid.Column="1"
                 Height="32"
                 VerticalContentAlignment="Center"
                 FontSize="14"
                 Margin="0,0,0,10"/>

        <!-- Hex选项 -->
        <CheckBox x:Name="chkHex"
                  Grid.Row="2" Grid.Column="1"
                  Content="Hex 发送"
                  VerticalAlignment="Center"
                  FontSize="13"
                  Margin="0,0,0,15"/>

        <!-- 分隔线 -->
        <Separator Grid.Row="3" Grid.ColumnSpan="2" Margin="0,5,0,15"/>

        <!-- 按钮 -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="btnOK"
                    Content="确定"
                    Width="75"
                    Height="32"
                    Margin="0,0,10,0"
                    Click="btnOK_Click"
                    IsDefault="True"
                    Background="#2196F3"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    FontWeight="SemiBold"/>
            <Button x:Name="btnCancel"
                    Content="取消"
                    Width="75"
                    Height="32"
                    Click="btnCancel_Click"
                    IsCancel="True"
                    Background="#E0E0E0"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
